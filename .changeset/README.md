# Changesets

This folder contains Changesets used to version and release the package.

Use:

```
npx changeset
```

Follow the prompts to select the version bump (patch/minor/major) and add a short summary.
